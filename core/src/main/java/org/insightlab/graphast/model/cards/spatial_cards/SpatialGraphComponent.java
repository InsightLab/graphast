package org.insightlab.graphast.model.cards.spatial_cards;

import org.insightlab.graphast.model.Graph;
import org.insightlab.graphast.model.cards.GraphComponent;

public class SpatialGraphComponent extends GraphComponent {
	
	public SpatialGraphComponent(Graph g) {
		this.setGraph(g);
	}

}
